## 第六周-编程题-16.18岁生日

1. 确定18岁生日年份,用出生年份加18得到18岁生日的年份：Y18 = 出生年份 + 18


2. 检查18岁生日是否存在
  * 如果出生日期是 2月29日：
    * 判断 Y18 是否为闰年：
        * 能被400整除，或能被4整除但不能被100整除 → 闰年
    * 如果 Y18 不是闰年，则该人没有18岁生日 → 输出 -1 并结束

  * 如果出生日期不是2月29日 → 继续下一步

3. 确定每个月的天数

* 1, 3, 5, 7, 8, 10, 12 月 → 31天

* 4, 6, 9, 11 月 → 30天

* 2月 → 闰年29天，平年28天

4. 计算从出生到18岁生日的总天数

初始化计数器 days = 0,
从出生日期开始逐天累加，直到到达18岁生日：
每天 days +1,
日期超过当月天数 → 日期置1，月份加1
月份超过12 → 月份置1，年份加1
循环结束时，days 即为总天数

```C++
#include <iostream>
using namespace std;

// 判断是否为闰年
bool isLeap(int year) {
    return (year % 400 == 0) || (year % 4 == 0 && year % 100 != 0);
}

// 获取某年某月的天数（使用 switch-case）
int daysInMonth(int year, int month) {
    switch (month) {
        case 1: case 3: case 5: case 7:
        case 8: case 10: case 12:
            return 31;
        case 4: case 6: case 9: case 11:
            return 30;
        case 2:
            return isLeap(year) ? 29 : 28;
        default:
            return 0; // 不存在的月份
    }
}

// 计算两日期间天数
int daysBetween(int y1,int m1,int d1,int y2,int m2,int d2){
    int days = 0;
    while (y1 < y2 || m1 < m2 || d1 < d2) {
        d1++;
        days++;
        if (d1 > daysInMonth(y1,m1)) {
            d1 = 1;
            m1++;
            if (m1 > 12) {
                m1 = 1;
                y1++;
            }
        }
    }
    return days;
}

int main() {
    int Y, M, D;
    cin >> Y >> M >> D;

    int Y18 = Y + 18;
    // 如果18岁生日是闰年2月29日，则没有18岁生日
    if (M == 2 && D == 29 && !isLeap(Y18)) {
        cout << -1 << endl;
        return 0;
    }

    int totalDays = daysBetween(Y, M, D, Y18, M, D);
    cout << totalDays << endl;

    return 0;
}

```

## 第六周-编程题-17.求出1～1 000之间的所有完全数

很容易发现这样一个事实：如果 𝑥 是 a的约数，那么a/x也是a的约数

所以枚举素数的时候，需要在[1,√𝑎]这个区间里枚举。


记录一个变量，将所有的因子都加起来，看看是否和自己相等
```C++
#include <iostream>
int sum_divs(int n) {
    if (n <= 1) return 0;

    int sum = 1; // 1 总是因子


    for (int i = 2; i*i <= n; ++i) {
        if (n % i == 0) {
            sum += i;       // 因子 i
            int j = n / i;  // 配对因子 j

            // 避免完全平方数重复相加
            if (i != j) {
                sum += j;
            }
        }
    }
    return sum;
}

int main() {
    const int MAX_LIMIT = 1000;
    // 遍历 1 到 1000
    for (int n = 1; n <= MAX_LIMIT; ++n) {
        // 检查是否为完全数
        if (sum_divs(n) == n) {
            // 输出完全数
            cout << n;
        }
    }
    return 0;
}
```


## 第六周-编程题-18.猴子分桃子

数学方法： 

1. 猴子分桃前有 $P$ 个桃子。
2. $P$ 必须满足 $P$ 除以 5 余 1（即 $P \equiv 1 \pmod 5$）。
3. 猴子扔掉 1 个，剩下 $P-1$ 个。
4. 猴子拿走 $\frac{1}{5}$ 份，留下 $\frac{4}{5}$ 份。
5. 下一只猴子分桃前的桃子数 $P_{\text{剩}} = \frac{4}{5} \times (P - 1)$。 
6. 
设 $X$ 为原始桃子数（即 $P_0$）。初始值：$M_0 = X + 4$。五次分桃后：$M_5 = \left(\frac{4}{5}\right)^5 M_0$。代入 $M_0$:$$M_5 = \frac{4^5}{5^5} (X + 4)$$
同时，我们知道 $M_5 = P_5 + 4$，其中 $P_5$ 是最后剩下的桃子数，它必须是一个整数。为了使 $M_5$ 和 $X$ 都是整数，$X+4$ 必须是 $5^5$ 的倍数。$$X + 4 = k \cdot 5^5 \quad (\text{其中 } k \text{ 为正整数})$$

为了求出最少的桃子数，我们取最小的正整数 $k=1$

以上的都是数学方法,以下是代码方法
```C++
#include <iostream>

int main() {
    int x = 6; // 初始尝试的桃子数，从最小可能值开始

    while (true) {
        int p = x;
        bool ok = true;

        // 模拟 5 只猴子分桃
        for (int i = 0; i < 5; ++i) {
            // 检查能否平均分为 5 份并多 1 个
            if (p % 5 != 1) {
                ok = false;
                break;
            }
            
            // 扔掉 1 个，剩下 4/5
            p = (p - 1) / 5 * 4; 
        }

        if (ok) {
            // 确保最后剩下的桃子数 p 至少是 4 (最后剩下的 4 份，每份至少 1 个)
            if (p >= 4) {
                 std::cout << x << std::endl;
                 return 0;
            }
        }
        
        x++; // 尝试下一个数
    }
    
    return 0;
}
```